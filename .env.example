# Copy to .env — never commit secrets

# Optional: CryptoCompare news API (Phase 1+)
CRYPTOCOMPARE_API_KEY=

# Bitvavo (Phase 3+) — leave empty for Phase 0–1
BITVAVO_API_KEY=
BITVAVO_API_SECRET=

# Database path override (default: data/advisor.db)
# ADVISOR_DB_PATH=data/advisor.db
